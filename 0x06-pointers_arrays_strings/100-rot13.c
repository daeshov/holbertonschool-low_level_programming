#include"holberton.h"
/**
 * rot13 - function that encodes a string
 * Return: always 0
 */

char *rot13(char *)
{

int m;
char encode[6] = {'r', 'o', 't', '1', '3', '\0'};
for (m = 0; m < encode; m++)
return (0);
}
